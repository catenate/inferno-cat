# |sort|uniq

header:
	cicd pastout $$1
	tail -f log/$$1.test.out
	
	regress skip
	
		> set -efux
	
		> echo done

regress:
	git grep -l 'regress skip'
	log $$1 regress

self:
	|aide testsh header $% |sed 's,^	,,'

