# |sort|uniq

comment:
	Edit s,^,#,g
	Edit s,^#,,g

gedit:
	host gedit scratch

indent:
	Edit s,^	,,g
	Edit s,^,	,g
	|sed 's,^	,,'
	|sed 's,^,	,'

replace:
	nsh replace -n a b `{git grep -l '{{ a }}'}
	replace a b `{git grep -l '{{ a }}'}

sort:
	|sort|uniq

tr:
	|tr A-Z a-z
