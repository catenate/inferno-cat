Dump /github/inferno-cat/dump
Edit ,|sort|uniq
Load /github/inferno-cat/dump
cat sh-inferno/* | grep -v '^[	 }{]*$' | grep -v '^[	 ]*#' |  wc -l
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
gedit scratch
git
git commit -a -m 'Update git and host to expect mounts under /usr/coinferno, so they''re not always part of the /usr/inferno tree.  This was useful to create at tar file of the /usr/inferno tree.  Add kanban to search for kanban files and match partial paths.'
git log --decorate --graph --oneline --all | sed 10q
log
log git
ls -F | mc
git add sh-inferno/man9
